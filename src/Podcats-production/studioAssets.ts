/**
 * Shared studio/brand assets that are not episode-specific.
 * To swap the background video: change backgroundVideo and put the
 * new file in public/ — nothing else needs to change.
 */
export const STUDIO_ASSETS = {
  backgroundVideo: "background.mp4",
} as const;
