{
    "sourceFile": "src/Podcats-production/VibeCodingDebate.tsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 5,
            "patches": [
                {
                    "date": 1774885197210,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1774885343297,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,35 +1,49 @@\n import React from \"react\";\n-import { AbsoluteFill, Audio, Sequence, interpolate, staticFile, useCurrentFrame, useVideoConfig } from \"remotion\";\n+import {\n+  AbsoluteFill,\n+  Audio,\n+  Sequence,\n+  interpolate,\n+  staticFile,\n+  useCurrentFrame,\n+  useVideoConfig,\n+} from \"remotion\";\n import { debateData, prepareSegments } from \"./data\";\n import { DebateSegment } from \"./DebateSegment\";\n import { HookBanner } from \"./HookBanner\";\n import { OutroBanner } from \"./OutroBanner\";\n \n-export const VibeCodingDebate: React.FC<{ type?: \"full\" | \"insight\" | \"atomic\"; startFrame?: number }> = ({ \n-  type = \"full\", \n-  startFrame = 0 \n-}) => {\n+export const VibeCodingDebate: React.FC<{\n+  type?: \"full\" | \"insight\" | \"atomic\";\n+  startFrame?: number;\n+}> = ({ type = \"full\", startFrame = 0 }) => {\n   const frame = useCurrentFrame();\n   const { fps, durationInFrames } = useVideoConfig();\n-  \n+\n   // Convert timestamps to frame numbers\n   const segments = prepareSegments(fps);\n   const distributionTargets = debateData.metadata.distribution_targets;\n   const isShortClip = type === \"insight\" || type === \"atomic\";\n \n   // Intro Hook logic\n   const HOOK_DURATION_FPS = fps * 3;\n-  const hookText = type === \"insight\" \n-    ? distributionTargets.linkedin_hook \n-    : type === \"atomic\" \n-    ? distributionTargets.tiktok_hook \n-    : null;\n+  const hookText =\n+    type === \"insight\"\n+      ? distributionTargets.linkedin_hook\n+      : type === \"atomic\"\n+        ? distributionTargets.tiktok_hook\n+        : null;\n   const outroText = isShortClip ? distributionTargets.short_outro_cta : null;\n   const outroDurationFrames = isShortClip\n     ? Math.round(distributionTargets.short_outro_duration_seconds * fps)\n     : 0;\n   const outroStartFrame = Math.max(durationInFrames - outroDurationFrames, 0);\n+  const contentOffsetFrames = hookText ? HOOK_DURATION_FPS : 0;\n+  const audioDurationFrames = Math.max(durationInFrames - contentOffsetFrames, 0);\n+  const visualDurationFrames = isShortClip\n+    ? Math.max(audioDurationFrames - outroDurationFrames, 0)\n+    : audioDurationFrames;\n   const audioVolume = isShortClip\n     ? interpolate(frame, [outroStartFrame, durationInFrames - 1], [1, 0], {\n         extrapolateLeft: \"clamp\",\n         extrapolateRight: \"clamp\",\n@@ -40,35 +54,55 @@\n     <AbsoluteFill style={{ backgroundColor: \"black\" }}>\n       {/* 1. The Hook Intro (only for social media clips) */}\n       {hookText && (\n         <Sequence durationInFrames={HOOK_DURATION_FPS}>\n-           <HookBanner text={hookText} type={type === \"insight\" ? \"insight\" : \"atomic\"} />\n+          <HookBanner\n+            text={hookText}\n+            type={type === \"insight\" ? \"insight\" : \"atomic\"}\n+          />\n         </Sequence>\n       )}\n \n-      {/* 2. The Main Conversation Sequence (offset by hook duration if applicable) */}\n-      <Sequence from={hookText ? HOOK_DURATION_FPS : 0}>\n+      {/* 2. Audio continues through the final fade-out of the short clip */}\n+      <Sequence from={contentOffsetFrames} durationInFrames={audioDurationFrames}>\n         <Sequence from={-startFrame}>\n           {/* Play the podcast audio track — continuous, uninterrupted */}\n-          <Audio src={staticFile(\"Vibe_coding_frente_al_rigor_técnico.m4a\")} volume={audioVolume} />\n+          <Audio\n+            src={staticFile(\"Vibe_coding_frente_al_rigor_técnico.m4a\")}\n+            volume={audioVolume}\n+          />\n \n+        </Sequence>\n+      </Sequence>\n+\n+      {/* 3. The Main Conversation visuals stop before the final outro */}\n+      <Sequence from={contentOffsetFrames} durationInFrames={visualDurationFrames}>\n+        <Sequence from={-startFrame}>\n+\n           {/* Position each segment at its ABSOLUTE frame position */}\n           {segments.map((segment, i: number) => (\n             <Sequence\n               key={i}\n               from={segment.startFrame}\n               durationInFrames={segment.durationFrames}\n               premountFor={10}\n             >\n-              <DebateSegment segmentProps={segment} segmentIndex={i} type={type} />\n+              <DebateSegment\n+                segmentProps={segment}\n+                segmentIndex={i}\n+                type={type}\n+              />\n             </Sequence>\n           ))}\n         </Sequence>\n       </Sequence>\n \n       {outroText && outroDurationFrames > 0 && (\n         <Sequence from={outroStartFrame} durationInFrames={outroDurationFrames}>\n-          <OutroBanner text={outroText} type={type === \"insight\" ? \"insight\" : \"atomic\"} />\n+          <OutroBanner\n+            text={outroText}\n+            type={type === \"insight\" ? \"insight\" : \"atomic\"}\n+          />\n         </Sequence>\n       )}\n     </AbsoluteFill>\n   );\n"
                },
                {
                    "date": 1774886735761,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -37,13 +37,28 @@\n   const outroDurationFrames = isShortClip\n     ? Math.round(distributionTargets.short_outro_duration_seconds * fps)\n     : 0;\n   const outroStartFrame = Math.max(durationInFrames - outroDurationFrames, 0);\n+  const outroOverlapFrames = isShortClip ? Math.round(fps * 0.4) : 0;\n   const contentOffsetFrames = hookText ? HOOK_DURATION_FPS : 0;\n-  const audioDurationFrames = Math.max(durationInFrames - contentOffsetFrames, 0);\n+  const audioDurationFrames = Math.max(\n+    durationInFrames - contentOffsetFrames,\n+    0,\n+  );\n   const visualDurationFrames = isShortClip\n-    ? Math.max(audioDurationFrames - outroDurationFrames, 0)\n+    ? Math.max(audioDurationFrames - outroDurationFrames + outroOverlapFrames, 0)\n     : audioDurationFrames;\n+  const visualOpacity = isShortClip\n+    ? interpolate(\n+        frame,\n+        [outroStartFrame, outroStartFrame + outroOverlapFrames],\n+        [1, 0],\n+        {\n+          extrapolateLeft: \"clamp\",\n+          extrapolateRight: \"clamp\",\n+        },\n+      )\n+    : 1;\n   const audioVolume = isShortClip\n     ? interpolate(frame, [outroStartFrame, durationInFrames - 1], [1, 0], {\n         extrapolateLeft: \"clamp\",\n         extrapolateRight: \"clamp\",\n@@ -62,39 +77,45 @@\n         </Sequence>\n       )}\n \n       {/* 2. Audio continues through the final fade-out of the short clip */}\n-      <Sequence from={contentOffsetFrames} durationInFrames={audioDurationFrames}>\n+      <Sequence\n+        from={contentOffsetFrames}\n+        durationInFrames={audioDurationFrames}\n+      >\n         <Sequence from={-startFrame}>\n           {/* Play the podcast audio track — continuous, uninterrupted */}\n           <Audio\n             src={staticFile(\"Vibe_coding_frente_al_rigor_técnico.m4a\")}\n             volume={audioVolume}\n           />\n-\n         </Sequence>\n       </Sequence>\n \n       {/* 3. The Main Conversation visuals stop before the final outro */}\n-      <Sequence from={contentOffsetFrames} durationInFrames={visualDurationFrames}>\n-        <Sequence from={-startFrame}>\n-\n-          {/* Position each segment at its ABSOLUTE frame position */}\n-          {segments.map((segment, i: number) => (\n-            <Sequence\n-              key={i}\n-              from={segment.startFrame}\n-              durationInFrames={segment.durationFrames}\n-              premountFor={10}\n-            >\n-              <DebateSegment\n-                segmentProps={segment}\n-                segmentIndex={i}\n-                type={type}\n-              />\n-            </Sequence>\n-          ))}\n-        </Sequence>\n+      <Sequence\n+        from={contentOffsetFrames}\n+        durationInFrames={visualDurationFrames}\n+      >\n+        <AbsoluteFill style={{ opacity: visualOpacity }}>\n+          <Sequence from={-startFrame}>\n+            {/* Position each segment at its ABSOLUTE frame position */}\n+            {segments.map((segment, i: number) => (\n+              <Sequence\n+                key={i}\n+                from={segment.startFrame}\n+                durationInFrames={segment.durationFrames}\n+                premountFor={10}\n+              >\n+                <DebateSegment\n+                  segmentProps={segment}\n+                  segmentIndex={i}\n+                  type={type}\n+                />\n+              </Sequence>\n+            ))}\n+          </Sequence>\n+        </AbsoluteFill>\n       </Sequence>\n \n       {outroText && outroDurationFrames > 0 && (\n         <Sequence from={outroStartFrame} durationInFrames={outroDurationFrames}>\n"
                },
                {
                    "date": 1774907999021,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -7,9 +7,9 @@\n   staticFile,\n   useCurrentFrame,\n   useVideoConfig,\n } from \"remotion\";\n-import { debateData, prepareSegments } from \"./data\";\n+import { debateData, prepareSegments } from \"./vibe-coding/assets/data\";\n import { DebateSegment } from \"./DebateSegment\";\n import { HookBanner } from \"./HookBanner\";\n import { OutroBanner } from \"./OutroBanner\";\n \n"
                },
                {
                    "date": 1774908998422,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -7,9 +7,10 @@\n   staticFile,\n   useCurrentFrame,\n   useVideoConfig,\n } from \"remotion\";\n-import { debateData, prepareSegments } from \"./vibe-coding/assets/data\";\n+import { currentPodcastEpisode } from \"./currentEpisode\";\n+import { prepareSegments } from \"./episode-schema\";\n import { DebateSegment } from \"./DebateSegment\";\n import { HookBanner } from \"./HookBanner\";\n import { OutroBanner } from \"./OutroBanner\";\n \n@@ -18,11 +19,12 @@\n   startFrame?: number;\n }> = ({ type = \"full\", startFrame = 0 }) => {\n   const frame = useCurrentFrame();\n   const { fps, durationInFrames } = useVideoConfig();\n+  const debateData = currentPodcastEpisode.data;\n \n   // Convert timestamps to frame numbers\n-  const segments = prepareSegments(fps);\n+  const segments = prepareSegments(debateData, fps);\n   const distributionTargets = debateData.metadata.distribution_targets;\n   const isShortClip = type === \"insight\" || type === \"atomic\";\n \n   // Intro Hook logic\n@@ -44,9 +46,12 @@\n     durationInFrames - contentOffsetFrames,\n     0,\n   );\n   const visualDurationFrames = isShortClip\n-    ? Math.max(audioDurationFrames - outroDurationFrames + outroOverlapFrames, 0)\n+    ? Math.max(\n+        audioDurationFrames - outroDurationFrames + outroOverlapFrames,\n+        0,\n+      )\n     : audioDurationFrames;\n   const visualOpacity = isShortClip\n     ? interpolate(\n         frame,\n@@ -84,9 +89,9 @@\n       >\n         <Sequence from={-startFrame}>\n           {/* Play the podcast audio track — continuous, uninterrupted */}\n           <Audio\n-            src={staticFile(\"Vibe_coding_frente_al_rigor_técnico.m4a\")}\n+            src={staticFile(debateData.metadata.audio_file)}\n             volume={audioVolume}\n           />\n         </Sequence>\n       </Sequence>\n"
                },
                {
                    "date": 1774981425756,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,8 +11,9 @@\n import { currentPodcastEpisode } from \"./currentEpisode\";\n import { prepareSegments } from \"./episode-schema\";\n import { DebateSegment } from \"./DebateSegment\";\n import { HookBanner } from \"./HookBanner\";\n+import { LogoCenterOverlay } from \"./KeywordHighlight\";\n import { OutroBanner } from \"./OutroBanner\";\n \n export const VibeCodingDebate: React.FC<{\n   type?: \"full\" | \"insight\" | \"atomic\";\n@@ -117,8 +118,24 @@\n                   type={type}\n                 />\n               </Sequence>\n             ))}\n+\n+            {/* Logo overlay spans ALL consecutive logo-mode segments as one continuous Sequence */}\n+            {(() => {\n+              const logoSegs = segments.filter(\n+                (s) => s.overlay_ui.center_mode === \"logo\",\n+              );\n+              if (logoSegs.length === 0) return null;\n+              const logoFrom = logoSegs[0].startFrame;\n+              const logoDuration =\n+                logoSegs[logoSegs.length - 1].endFrame - logoFrom;\n+              return (\n+                <Sequence from={logoFrom} durationInFrames={logoDuration}>\n+                  <LogoCenterOverlay durationFrames={logoDuration} />\n+                </Sequence>\n+              );\n+            })()}\n           </Sequence>\n         </AbsoluteFill>\n       </Sequence>\n \n"
                }
            ],
            "date": 1774885197210,
            "name": "Commit-0",
            "content": "import React from \"react\";\nimport { AbsoluteFill, Audio, Sequence, interpolate, staticFile, useCurrentFrame, useVideoConfig } from \"remotion\";\nimport { debateData, prepareSegments } from \"./data\";\nimport { DebateSegment } from \"./DebateSegment\";\nimport { HookBanner } from \"./HookBanner\";\nimport { OutroBanner } from \"./OutroBanner\";\n\nexport const VibeCodingDebate: React.FC<{ type?: \"full\" | \"insight\" | \"atomic\"; startFrame?: number }> = ({ \n  type = \"full\", \n  startFrame = 0 \n}) => {\n  const frame = useCurrentFrame();\n  const { fps, durationInFrames } = useVideoConfig();\n  \n  // Convert timestamps to frame numbers\n  const segments = prepareSegments(fps);\n  const distributionTargets = debateData.metadata.distribution_targets;\n  const isShortClip = type === \"insight\" || type === \"atomic\";\n\n  // Intro Hook logic\n  const HOOK_DURATION_FPS = fps * 3;\n  const hookText = type === \"insight\" \n    ? distributionTargets.linkedin_hook \n    : type === \"atomic\" \n    ? distributionTargets.tiktok_hook \n    : null;\n  const outroText = isShortClip ? distributionTargets.short_outro_cta : null;\n  const outroDurationFrames = isShortClip\n    ? Math.round(distributionTargets.short_outro_duration_seconds * fps)\n    : 0;\n  const outroStartFrame = Math.max(durationInFrames - outroDurationFrames, 0);\n  const audioVolume = isShortClip\n    ? interpolate(frame, [outroStartFrame, durationInFrames - 1], [1, 0], {\n        extrapolateLeft: \"clamp\",\n        extrapolateRight: \"clamp\",\n      })\n    : 1;\n\n  return (\n    <AbsoluteFill style={{ backgroundColor: \"black\" }}>\n      {/* 1. The Hook Intro (only for social media clips) */}\n      {hookText && (\n        <Sequence durationInFrames={HOOK_DURATION_FPS}>\n           <HookBanner text={hookText} type={type === \"insight\" ? \"insight\" : \"atomic\"} />\n        </Sequence>\n      )}\n\n      {/* 2. The Main Conversation Sequence (offset by hook duration if applicable) */}\n      <Sequence from={hookText ? HOOK_DURATION_FPS : 0}>\n        <Sequence from={-startFrame}>\n          {/* Play the podcast audio track — continuous, uninterrupted */}\n          <Audio src={staticFile(\"Vibe_coding_frente_al_rigor_técnico.m4a\")} volume={audioVolume} />\n\n          {/* Position each segment at its ABSOLUTE frame position */}\n          {segments.map((segment, i: number) => (\n            <Sequence\n              key={i}\n              from={segment.startFrame}\n              durationInFrames={segment.durationFrames}\n              premountFor={10}\n            >\n              <DebateSegment segmentProps={segment} segmentIndex={i} type={type} />\n            </Sequence>\n          ))}\n        </Sequence>\n      </Sequence>\n\n      {outroText && outroDurationFrames > 0 && (\n        <Sequence from={outroStartFrame} durationInFrames={outroDurationFrames}>\n          <OutroBanner text={outroText} type={type === \"insight\" ? \"insight\" : \"atomic\"} />\n        </Sequence>\n      )}\n    </AbsoluteFill>\n  );\n};\n"
        }
    ]
}