{
    "sourceFile": "src/Podcats-production/Subtitle.tsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 9,
            "patches": [
                {
                    "date": 1774948956005,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1774949087045,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -103,9 +103,9 @@\n               >\n                 {word}\n               </span>\n             );\n-          })}}\n+          })}\n         </div>\n       </div>\n     </div>\n   );\n"
                },
                {
                    "date": 1774949469409,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -25,9 +25,9 @@\n   const wordsToShow = Math.floor(\n     interpolate(frame, [10, revealEnd], [0, totalWords], {\n       extrapolateLeft: \"clamp\",\n       extrapolateRight: \"clamp\",\n-    })\n+    }),\n   );\n \n   // Container entrance spring\n   const containerSpring = spring({\n@@ -43,9 +43,9 @@\n   const exitOpacity = interpolate(\n     frame,\n     [durationFrames - 10, durationFrames],\n     [1, 0],\n-    { extrapolateLeft: \"clamp\", extrapolateRight: \"clamp\" }\n+    { extrapolateLeft: \"clamp\", extrapolateRight: \"clamp\" },\n   );\n \n   const borderColor = SPEAKER_BORDER_COLORS[speakerId];\n \n@@ -68,10 +68,31 @@\n           borderRadius: 16,\n           padding: \"28px 36px\",\n           borderLeft: `6px solid ${borderColor}`,\n           boxShadow: `0 0 30px ${borderColor}33, 0 8px 32px rgba(0,0,0,0.5), inset 0 0 0 1px rgba(255,255,255,0.06)`,\n+          position: \"relative\",\n         }}\n       >\n+        {/* Corner brackets in speaker color */}\n+        {(\n+          [\n+            { top: 8,    left: 8,    borderTop: `2px solid ${borderColor}cc`, borderLeft:   `2px solid ${borderColor}cc` },\n+            { top: 8,    right: 8,   borderTop: `2px solid ${borderColor}cc`, borderRight:  `2px solid ${borderColor}cc` },\n+            { bottom: 8, left: 8,    borderBottom: `2px solid ${borderColor}cc`, borderLeft:  `2px solid ${borderColor}cc` },\n+            { bottom: 8, right: 8,   borderBottom: `2px solid ${borderColor}cc`, borderRight: `2px solid ${borderColor}cc` },\n+          ] as React.CSSProperties[]\n+        ).map((bracketStyle, i) => (\n+          <div\n+            key={i}\n+            style={{\n+              position: \"absolute\",\n+              width: 20,\n+              height: 20,\n+              filter: `drop-shadow(0 0 4px ${borderColor}99)`,\n+              ...bracketStyle,\n+            }}\n+          />\n+        ))}\n         <div\n           style={{\n             fontFamily: '\"Courier New\", \"JetBrains Mono\", monospace',\n             fontSize: 34,\n@@ -96,9 +117,11 @@\n                       ? borderColor\n                       : \"rgba(255,255,255,0.95)\"\n                     : \"rgba(255,255,255,0.1)\",\n                   fontWeight: isJustAppeared ? 700 : 500,\n-                  textShadow: isJustAppeared ? `0 0 12px ${borderColor}, 0 0 24px ${borderColor}66` : \"none\",\n+                  textShadow: isJustAppeared\n+                    ? `0 0 12px ${borderColor}, 0 0 24px ${borderColor}66`\n+                    : \"none\",\n                   transition: \"color 0.1s\",\n                 }}\n               >\n                 {word}\n"
                },
                {
                    "date": 1774950727224,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,19 +1,29 @@\n import React from \"react\";\n-import { interpolate, spring, useCurrentFrame, useVideoConfig } from \"remotion\";\n+import { Img, interpolate, spring, staticFile, useCurrentFrame, useVideoConfig } from \"remotion\";\n \n-const SPEAKER_BORDER_COLORS = {\n-  0: \"#4facfe\",\n-  1: \"#f093fb\",\n-} as const;\n+const SPEAKER_DIALOG: Record<0 | 1, { svg: string; glow: string; accent: string }> = {\n+  0: { svg: staticFile(\"dialogbox-blue.svg\"),  glow: \"rgba(79,172,254,0.55)\",   accent: \"#4facfe\" },\n+  1: { svg: staticFile(\"dialogbox-pink.svg\"),  glow: \"rgba(240,147,251,0.55)\", accent: \"#f093fb\" },\n+};\n \n+// Both SVGs use a 2000×2000 viewBox.\n+// The dialog polygon bounds (blue: y 641-1358, pink: y 637-1302).\n+// DIALOG_IMG_HEIGHT is chosen so that the dialog region fits exactly in DIALOG_BOX_HEIGHT.\n+//   scale = DIALOG_IMG_HEIGHT / 2000  →  dialog (~717 units) × scale ≈ 200px\n+// DIALOG_TOP_OFFSET shifts the img up so the dialog top aligns with the container top.\n+const DIALOG_BOX_HEIGHT = 200;   // visible container height (px)\n+const DIALOG_IMG_HEIGHT = 558;   // rendered img height: 2000 × (200/717) ≈ 558px\n+const DIALOG_TOP_OFFSET  = -179; // -(641 × 558/2000) ≈ -179px\n+\n export const Subtitle: React.FC<{\n   text: string;\n   speakerId: 0 | 1;\n   durationFrames: number;\n }> = ({ text, speakerId, durationFrames }) => {\n   const frame = useCurrentFrame();\n   const { fps } = useVideoConfig();\n+  const { svg, glow, accent } = SPEAKER_DIALOG[speakerId];\n \n   // Split text into words\n   const words = text.split(/\\s+/);\n   const totalWords = words.length;\n@@ -46,10 +56,8 @@\n     [1, 0],\n     { extrapolateLeft: \"clamp\", extrapolateRight: \"clamp\" },\n   );\n \n-  const borderColor = SPEAKER_BORDER_COLORS[speakerId];\n-\n   return (\n     <div\n       style={{\n         position: \"absolute\",\n@@ -59,76 +67,81 @@\n         transform: `translateY(${containerY}px)`,\n         opacity: containerOpacity * exitOpacity,\n       }}\n     >\n+      {/* Dialog box container — clips the oversized SVG to show only the dialog region */}\n       <div\n         style={{\n-          backgroundColor: \"rgba(4, 7, 18, 0.82)\",\n-          backdropFilter: \"blur(8px)\",\n-          WebkitBackdropFilter: \"blur(8px)\",\n-          borderRadius: 16,\n-          padding: \"28px 36px\",\n-          borderLeft: `6px solid ${borderColor}`,\n-          boxShadow: `0 0 30px ${borderColor}33, 0 8px 32px rgba(0,0,0,0.5), inset 0 0 0 1px rgba(255,255,255,0.06)`,\n           position: \"relative\",\n+          height: DIALOG_BOX_HEIGHT,\n+          overflow: \"hidden\",\n+          // drop-shadow respects SVG transparency → glows around the dialog shape\n+          filter: `drop-shadow(0 0 22px ${glow}) drop-shadow(0 10px 40px rgba(0,0,0,0.7))`,\n         }}\n       >\n-        {/* Corner brackets in speaker color */}\n-        {(\n-          [\n-            { top: 8,    left: 8,    borderTop: `2px solid ${borderColor}cc`, borderLeft:   `2px solid ${borderColor}cc` },\n-            { top: 8,    right: 8,   borderTop: `2px solid ${borderColor}cc`, borderRight:  `2px solid ${borderColor}cc` },\n-            { bottom: 8, left: 8,    borderBottom: `2px solid ${borderColor}cc`, borderLeft:  `2px solid ${borderColor}cc` },\n-            { bottom: 8, right: 8,   borderBottom: `2px solid ${borderColor}cc`, borderRight: `2px solid ${borderColor}cc` },\n-          ] as React.CSSProperties[]\n-        ).map((bracketStyle, i) => (\n-          <div\n-            key={i}\n-            style={{\n-              position: \"absolute\",\n-              width: 20,\n-              height: 20,\n-              filter: `drop-shadow(0 0 4px ${borderColor}99)`,\n-              ...bracketStyle,\n-            }}\n-          />\n-        ))}\n+        {/* SVG rendered tall enough so its dialog polygon region fills DIALOG_BOX_HEIGHT,\n+            then shifted up by DIALOG_TOP_OFFSET to align the dialog top with the container top.\n+            objectFit: fill stretches the square SVG to match container width exactly. */}\n+        <Img\n+          src={svg}\n+          style={{\n+            position: \"absolute\",\n+            left: 0,\n+            width: \"100%\",\n+            height: DIALOG_IMG_HEIGHT,\n+            top: DIALOG_TOP_OFFSET,\n+            objectFit: \"fill\",\n+          }}\n+        />\n+\n+        {/* Text content — sits over the SVG dialog fill */}\n         <div\n           style={{\n-            fontFamily: '\"Courier New\", \"JetBrains Mono\", monospace',\n-            fontSize: 34,\n-            color: \"white\",\n-            lineHeight: 1.6,\n+            position: \"relative\",\n+            zIndex: 1,\n+            padding: \"16px 240px\",\n+            height: \"100%\",\n+            boxSizing: \"border-box\",\n             display: \"flex\",\n-            flexWrap: \"wrap\",\n-            gap: \"0 8px\",\n+            alignItems: \"center\",\n           }}\n         >\n-          {words.map((word, i) => {\n-            const isVisible = i < wordsToShow;\n-            const isJustAppeared = i === wordsToShow - 1;\n+          <div\n+            style={{\n+              fontFamily: '\"Courier New\", \"JetBrains Mono\", monospace',\n+              fontSize: 30,\n+              color: \"white\",\n+              lineHeight: 1.55,\n+              display: \"flex\",\n+              flexWrap: \"wrap\",\n+              gap: \"0 8px\",\n+            }}\n+          >\n+            {words.map((word, i) => {\n+              const isVisible = i < wordsToShow;\n+              const isJustAppeared = i === wordsToShow - 1;\n \n-            return (\n-              <span\n-                key={i}\n-                style={{\n-                  opacity: isVisible ? 1 : 0.12,\n-                  color: isVisible\n-                    ? isJustAppeared\n-                      ? borderColor\n-                      : \"rgba(255,255,255,0.95)\"\n-                    : \"rgba(255,255,255,0.1)\",\n-                  fontWeight: isJustAppeared ? 700 : 500,\n-                  textShadow: isJustAppeared\n-                    ? `0 0 12px ${borderColor}, 0 0 24px ${borderColor}66`\n-                    : \"none\",\n-                  transition: \"color 0.1s\",\n-                }}\n-              >\n-                {word}\n-              </span>\n-            );\n-          })}\n+              return (\n+                <span\n+                  key={i}\n+                  style={{\n+                    opacity: isVisible ? 1 : 0.18,\n+                    color: isVisible\n+                      ? isJustAppeared\n+                        ? accent\n+                        : \"rgba(255,255,255,0.97)\"\n+                      : \"rgba(255,255,255,0.1)\",\n+                    fontWeight: isJustAppeared ? 700 : 500,\n+                    textShadow: isJustAppeared\n+                      ? `0 0 12px ${accent}, 0 0 24px ${accent}66`\n+                      : \"none\",\n+                  }}\n+                >\n+                  {word}\n+                </span>\n+              );\n+            })}\n+          </div>\n         </div>\n       </div>\n     </div>\n   );\n"
                },
                {
                    "date": 1774951126235,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,20 +1,38 @@\n import React from \"react\";\n-import { Img, interpolate, spring, staticFile, useCurrentFrame, useVideoConfig } from \"remotion\";\n+import {\n+  Img,\n+  interpolate,\n+  spring,\n+  staticFile,\n+  useCurrentFrame,\n+  useVideoConfig,\n+} from \"remotion\";\n \n-const SPEAKER_DIALOG: Record<0 | 1, { svg: string; glow: string; accent: string }> = {\n-  0: { svg: staticFile(\"dialogbox-blue.svg\"),  glow: \"rgba(79,172,254,0.55)\",   accent: \"#4facfe\" },\n-  1: { svg: staticFile(\"dialogbox-pink.svg\"),  glow: \"rgba(240,147,251,0.55)\", accent: \"#f093fb\" },\n+const SPEAKER_DIALOG: Record<\n+  0 | 1,\n+  { svg: string; glow: string; accent: string }\n+> = {\n+  0: {\n+    svg: staticFile(\"dialogbox-blue.svg\"),\n+    glow: \"rgba(79,172,254,0.55)\",\n+    accent: \"#4facfe\",\n+  },\n+  1: {\n+    svg: staticFile(\"dialogbox-pink.svg\"),\n+    glow: \"rgba(240,147,251,0.55)\",\n+    accent: \"#f093fb\",\n+  },\n };\n \n // Both SVGs use a 2000×2000 viewBox.\n // The dialog polygon bounds (blue: y 641-1358, pink: y 637-1302).\n // DIALOG_IMG_HEIGHT is chosen so that the dialog region fits exactly in DIALOG_BOX_HEIGHT.\n-//   scale = DIALOG_IMG_HEIGHT / 2000  →  dialog (~717 units) × scale ≈ 200px\n+//   scale = DIALOG_IMG_HEIGHT / 2000  →  dialog (~717 units) × scale = DIALOG_BOX_HEIGHT\n // DIALOG_TOP_OFFSET shifts the img up so the dialog top aligns with the container top.\n-const DIALOG_BOX_HEIGHT = 200;   // visible container height (px)\n-const DIALOG_IMG_HEIGHT = 558;   // rendered img height: 2000 × (200/717) ≈ 558px\n-const DIALOG_TOP_OFFSET  = -179; // -(641 × 558/2000) ≈ -179px\n+const DIALOG_BOX_HEIGHT = 320; // visible container height (px)\n+const DIALOG_IMG_HEIGHT = 894; // rendered img height: 2000 × (320/717) ≈ 894px\n+const DIALOG_TOP_OFFSET = -287; // -(641 × 894/2000) ≈ -287px\n \n export const Subtitle: React.FC<{\n   text: string;\n   speakerId: 0 | 1;\n"
                },
                {
                    "date": 1774951873107,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -23,16 +23,11 @@\n     accent: \"#f093fb\",\n   },\n };\n \n-// Both SVGs use a 2000×2000 viewBox.\n-// The dialog polygon bounds (blue: y 641-1358, pink: y 637-1302).\n-// DIALOG_IMG_HEIGHT is chosen so that the dialog region fits exactly in DIALOG_BOX_HEIGHT.\n-//   scale = DIALOG_IMG_HEIGHT / 2000  →  dialog (~717 units) × scale = DIALOG_BOX_HEIGHT\n-// DIALOG_TOP_OFFSET shifts the img up so the dialog top aligns with the container top.\n-const DIALOG_BOX_HEIGHT = 320; // visible container height (px)\n-const DIALOG_IMG_HEIGHT = 894; // rendered img height: 2000 × (320/717) ≈ 894px\n-const DIALOG_TOP_OFFSET = -287; // -(641 × 894/2000) ≈ -287px\n+// SVG viewBox cropped to dialog polygon region (viewBox=\"150 590 1700 860\")\n+// with preserveAspectRatio=\"none\" → stretches to fill container exactly.\n+// No offset math needed — the SVG renders at 100% × 100% of the container.\n \n export const Subtitle: React.FC<{\n   text: string;\n   speakerId: 0 | 1;\n@@ -78,50 +73,42 @@\n   return (\n     <div\n       style={{\n         position: \"absolute\",\n-        bottom: 60,\n-        left: 60,\n-        right: 60,\n+        bottom: 40,\n+        left: 0,\n+        right: 0,\n         transform: `translateY(${containerY}px)`,\n         opacity: containerOpacity * exitOpacity,\n       }}\n     >\n-      {/* Dialog box container — clips the oversized SVG to show only the dialog region */}\n+      {/* Dialog box — SVG stretches to fill container, polygon covers full area */}\n       <div\n         style={{\n           position: \"relative\",\n-          height: DIALOG_BOX_HEIGHT,\n-          overflow: \"hidden\",\n-          // drop-shadow respects SVG transparency → glows around the dialog shape\n           filter: `drop-shadow(0 0 22px ${glow}) drop-shadow(0 10px 40px rgba(0,0,0,0.7))`,\n         }}\n       >\n-        {/* SVG rendered tall enough so its dialog polygon region fills DIALOG_BOX_HEIGHT,\n-            then shifted up by DIALOG_TOP_OFFSET to align the dialog top with the container top.\n-            objectFit: fill stretches the square SVG to match container width exactly. */}\n         <Img\n           src={svg}\n           style={{\n             position: \"absolute\",\n-            left: 0,\n+            inset: 0,\n             width: \"100%\",\n-            height: DIALOG_IMG_HEIGHT,\n-            top: DIALOG_TOP_OFFSET,\n-            objectFit: \"fill\",\n+            height: \"100%\",\n           }}\n         />\n \n         {/* Text content — sits over the SVG dialog fill */}\n         <div\n           style={{\n             position: \"relative\",\n             zIndex: 1,\n-            padding: \"16px 240px\",\n-            height: \"100%\",\n+            padding: \"28px 120px\",\n             boxSizing: \"border-box\",\n             display: \"flex\",\n             alignItems: \"center\",\n+            minHeight: 160,\n           }}\n         >\n           <div\n             style={{\n"
                },
                {
                    "date": 1774952248430,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -102,13 +102,13 @@\n         <div\n           style={{\n             position: \"relative\",\n             zIndex: 1,\n-            padding: \"28px 120px\",\n+            padding: \"40px 120px\",\n             boxSizing: \"border-box\",\n             display: \"flex\",\n             alignItems: \"center\",\n-            minHeight: 160,\n+            minHeight: 300,\n           }}\n         >\n           <div\n             style={{\n"
                },
                {
                    "date": 1774952474975,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,50 +1,120 @@\n import React from \"react\";\n-import {\n-  Img,\n-  interpolate,\n-  spring,\n-  staticFile,\n-  useCurrentFrame,\n-  useVideoConfig,\n-} from \"remotion\";\n+import { interpolate, spring, useCurrentFrame, useVideoConfig } from \"remotion\";\n \n-const SPEAKER_DIALOG: Record<\n-  0 | 1,\n-  { svg: string; glow: string; accent: string }\n-> = {\n+const SPEAKER_THEME = {\n   0: {\n-    svg: staticFile(\"dialogbox-blue.svg\"),\n+    fill: \"#0D1282\",\n+    accent: \"#4facfe\",\n+    accentSecondary: \"#00f2fe\",\n     glow: \"rgba(79,172,254,0.55)\",\n-    accent: \"#4facfe\",\n   },\n   1: {\n-    svg: staticFile(\"dialogbox-pink.svg\"),\n+    fill: \"#4a0e6e\",\n+    accent: \"#f093fb\",\n+    accentSecondary: \"#f5576c\",\n     glow: \"rgba(240,147,251,0.55)\",\n-    accent: \"#f093fb\",\n   },\n+} as const;\n+\n+/**\n+ * Sci-fi dialog box SVG — generated inline so it adapts to any text size.\n+ * Shape: angular HUD panel with clipped corners, diagonal speed-lines at top,\n+ * side bar accent, and triangular decorations at bottom.\n+ * All coordinates in a 1000×1000 viewBox with preserveAspectRatio=\"none\".\n+ */\n+const DialogBoxSVG: React.FC<{\n+  fill: string;\n+  accent: string;\n+  accentSecondary: string;\n+  side: \"left\" | \"right\";\n+}> = ({ fill, accent, accentSecondary, side }) => {\n+  // Main panel — angular HUD shape\n+  // Offset the notch to the speaker side\n+  const isLeft = side === \"left\";\n+\n+  // Main body polygon: clipped corners + side notch\n+  const mainBody = isLeft\n+    ? \"50,0 960,0 1000,50 1000,950 960,1000 120,1000 60,930 0,930 0,840 40,810 40,70 0,50\"\n+    : \"40,0 950,0 1000,50 1000,930 960,930 960,810 1000,840 1000,70 960,50 960,1000 880,1000 40,1000 0,950 0,50\";\n+\n+  // Speed lines at top — 6 diagonal slashes\n+  const speedLines = Array.from({ length: 6 }, (_, i) => {\n+    const x = isLeft ? 120 + i * 70 : 880 - i * 70;\n+    return `M${x},0 L${x - 55},90 L${x - 25},90 L${x + 30},0 Z`;\n+  }).join(\" \");\n+\n+  // Bottom decoration — triangular accents\n+  const bottomDecor = isLeft\n+    ? \"M120,1000 L160,1060 L550,1060 L510,1000\"\n+    : \"M880,1000 L840,1060 L450,1060 L490,1000\";\n+\n+  // Side accent bar\n+  const sideBar = isLeft\n+    ? \"M0,130 L30,160 L30,750 L0,780\"\n+    : \"M1000,130 L970,160 L970,750 L1000,780\";\n+\n+  return (\n+    <svg\n+      viewBox={isLeft ? \"0 -10 1010 1080\" : \"-10 -10 1020 1080\"}\n+      preserveAspectRatio=\"none\"\n+      style={{ position: \"absolute\", inset: 0, width: \"100%\", height: \"100%\" }}\n+    >\n+      {/* Gradient for fills */}\n+      <defs>\n+        <linearGradient id={`dialogGrad-${side}`} x1=\"0%\" y1=\"0%\" x2=\"100%\" y2=\"100%\">\n+          <stop offset=\"0%\" stopColor={fill} />\n+          <stop offset=\"100%\" stopColor={fill} stopOpacity=\"0.85\" />\n+        </linearGradient>\n+      </defs>\n+\n+      {/* Main body */}\n+      <polygon points={mainBody} fill={`url(#dialogGrad-${side})`} />\n+\n+      {/* Border glow */}\n+      <polygon\n+        points={mainBody}\n+        fill=\"none\"\n+        stroke={accent}\n+        strokeWidth=\"2.5\"\n+        strokeOpacity=\"0.6\"\n+      />\n+\n+      {/* Speed lines at top */}\n+      <path d={speedLines} fill={accentSecondary} fillOpacity=\"0.7\" />\n+\n+      {/* Bottom decoration */}\n+      <path d={bottomDecor} fill={accent} fillOpacity=\"0.55\" />\n+\n+      {/* Side accent bar */}\n+      <path d={sideBar} fill={accent} fillOpacity=\"0.7\" />\n+\n+      {/* Inner border (subtle) */}\n+      <polygon\n+        points={mainBody}\n+        fill=\"none\"\n+        stroke=\"rgba(255,255,255,0.06)\"\n+        strokeWidth=\"1\"\n+        transform=\"scale(0.985) translate(7.5, 7.5)\"\n+      />\n+    </svg>\n+  );\n };\n \n-// SVG viewBox cropped to dialog polygon region (viewBox=\"150 590 1700 860\")\n-// with preserveAspectRatio=\"none\" → stretches to fill container exactly.\n-// No offset math needed — the SVG renders at 100% × 100% of the container.\n-\n export const Subtitle: React.FC<{\n   text: string;\n   speakerId: 0 | 1;\n   durationFrames: number;\n }> = ({ text, speakerId, durationFrames }) => {\n   const frame = useCurrentFrame();\n   const { fps } = useVideoConfig();\n-  const { svg, glow, accent } = SPEAKER_DIALOG[speakerId];\n+  const theme = SPEAKER_THEME[speakerId];\n+  const side = speakerId === 0 ? \"left\" : \"right\";\n \n   // Split text into words\n   const words = text.split(/\\s+/);\n   const totalWords = words.length;\n \n-  // Calculate how many words should be visible at the current frame.\n-  // Start revealing at frame 10, finish at 85% of the segment duration\n-  // to give time for reading the last words.\n   const revealEnd = Math.max(durationFrames * 0.85, 30);\n   const wordsToShow = Math.floor(\n     interpolate(frame, [10, revealEnd], [0, totalWords], {\n       extrapolateLeft: \"clamp\",\n@@ -80,35 +150,31 @@\n         transform: `translateY(${containerY}px)`,\n         opacity: containerOpacity * exitOpacity,\n       }}\n     >\n-      {/* Dialog box — SVG stretches to fill container, polygon covers full area */}\n       <div\n         style={{\n           position: \"relative\",\n-          filter: `drop-shadow(0 0 22px ${glow}) drop-shadow(0 10px 40px rgba(0,0,0,0.7))`,\n+          filter: `drop-shadow(0 0 22px ${theme.glow}) drop-shadow(0 10px 40px rgba(0,0,0,0.7))`,\n         }}\n       >\n-        <Img\n-          src={svg}\n-          style={{\n-            position: \"absolute\",\n-            inset: 0,\n-            width: \"100%\",\n-            height: \"100%\",\n-          }}\n+        <DialogBoxSVG\n+          fill={theme.fill}\n+          accent={theme.accent}\n+          accentSecondary={theme.accentSecondary}\n+          side={side}\n         />\n \n-        {/* Text content — sits over the SVG dialog fill */}\n+        {/* Text content */}\n         <div\n           style={{\n             position: \"relative\",\n             zIndex: 1,\n-            padding: \"40px 120px\",\n+            padding: \"36px 80px\",\n             boxSizing: \"border-box\",\n             display: \"flex\",\n             alignItems: \"center\",\n-            minHeight: 300,\n+            minHeight: 180,\n           }}\n         >\n           <div\n             style={{\n@@ -131,14 +197,14 @@\n                   style={{\n                     opacity: isVisible ? 1 : 0.18,\n                     color: isVisible\n                       ? isJustAppeared\n-                        ? accent\n+                        ? theme.accent\n                         : \"rgba(255,255,255,0.97)\"\n                       : \"rgba(255,255,255,0.1)\",\n                     fontWeight: isJustAppeared ? 700 : 500,\n                     textShadow: isJustAppeared\n-                      ? `0 0 12px ${accent}, 0 0 24px ${accent}66`\n+                      ? `0 0 12px ${theme.accent}, 0 0 24px ${theme.accent}66`\n                       : \"none\",\n                   }}\n                 >\n                   {word}\n"
                },
                {
                    "date": 1774952670781,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,38 +31,32 @@\n   // Main panel — angular HUD shape\n   // Offset the notch to the speaker side\n   const isLeft = side === \"left\";\n \n-  // Main body polygon: clipped corners + side notch\n-  const mainBody = isLeft\n-    ? \"50,0 960,0 1000,50 1000,950 960,1000 120,1000 60,930 0,930 0,840 40,810 40,70 0,50\"\n-    : \"40,0 950,0 1000,50 1000,930 960,930 960,810 1000,840 1000,70 960,50 960,1000 880,1000 40,1000 0,950 0,50\";\n+  // Main body polygon: clean angular panel with clipped corners only\n+  const mainBody =\n+    \"40,0 960,0 1000,40 1000,960 960,1000 40,1000 0,960 0,40\";\n \n-  // Speed lines at top — 6 diagonal slashes\n+  // Speed lines at top — 6 diagonal slashes near speaker side\n   const speedLines = Array.from({ length: 6 }, (_, i) => {\n-    const x = isLeft ? 120 + i * 70 : 880 - i * 70;\n-    return `M${x},0 L${x - 55},90 L${x - 25},90 L${x + 30},0 Z`;\n+    const x = isLeft ? 80 + i * 65 : 920 - i * 65;\n+    return `M${x},0 L${x - 45},70 L${x - 20},70 L${x + 25},0 Z`;\n   }).join(\" \");\n \n-  // Bottom decoration — triangular accents\n-  const bottomDecor = isLeft\n-    ? \"M120,1000 L160,1060 L550,1060 L510,1000\"\n-    : \"M880,1000 L840,1060 L450,1060 L490,1000\";\n-\n-  // Side accent bar\n-  const sideBar = isLeft\n-    ? \"M0,130 L30,160 L30,750 L0,780\"\n-    : \"M1000,130 L970,160 L970,750 L1000,780\";\n-\n   return (\n     <svg\n-      viewBox={isLeft ? \"0 -10 1010 1080\" : \"-10 -10 1020 1080\"}\n+      viewBox=\"0 0 1000 1000\"\n       preserveAspectRatio=\"none\"\n       style={{ position: \"absolute\", inset: 0, width: \"100%\", height: \"100%\" }}\n     >\n-      {/* Gradient for fills */}\n       <defs>\n-        <linearGradient id={`dialogGrad-${side}`} x1=\"0%\" y1=\"0%\" x2=\"100%\" y2=\"100%\">\n+        <linearGradient\n+          id={`dialogGrad-${side}`}\n+          x1=\"0%\"\n+          y1=\"0%\"\n+          x2=\"100%\"\n+          y2=\"100%\"\n+        >\n           <stop offset=\"0%\" stopColor={fill} />\n           <stop offset=\"100%\" stopColor={fill} stopOpacity=\"0.85\" />\n         </linearGradient>\n       </defs>\n@@ -81,14 +75,8 @@\n \n       {/* Speed lines at top */}\n       <path d={speedLines} fill={accentSecondary} fillOpacity=\"0.7\" />\n \n-      {/* Bottom decoration */}\n-      <path d={bottomDecor} fill={accent} fillOpacity=\"0.55\" />\n-\n-      {/* Side accent bar */}\n-      <path d={sideBar} fill={accent} fillOpacity=\"0.7\" />\n-\n       {/* Inner border (subtle) */}\n       <polygon\n         points={mainBody}\n         fill=\"none\"\n"
                },
                {
                    "date": 1774980040703,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,6 +1,7 @@\n import React from \"react\";\n import { interpolate, spring, useCurrentFrame, useVideoConfig } from \"remotion\";\n+import type { WordTiming } from \"./episode-schema\";\n \n const SPEAKER_THEME = {\n   0: {\n     fill: \"#0D1282\",\n@@ -15,29 +16,16 @@\n     glow: \"rgba(240,147,251,0.55)\",\n   },\n } as const;\n \n-/**\n- * Sci-fi dialog box SVG — generated inline so it adapts to any text size.\n- * Shape: angular HUD panel with clipped corners, diagonal speed-lines at top,\n- * side bar accent, and triangular decorations at bottom.\n- * All coordinates in a 1000×1000 viewBox with preserveAspectRatio=\"none\".\n- */\n const DialogBoxSVG: React.FC<{\n   fill: string;\n   accent: string;\n   accentSecondary: string;\n   side: \"left\" | \"right\";\n }> = ({ fill, accent, accentSecondary, side }) => {\n-  // Main panel — angular HUD shape\n-  // Offset the notch to the speaker side\n   const isLeft = side === \"left\";\n-\n-  // Main body polygon: clean angular panel with clipped corners only\n-  const mainBody =\n-    \"40,0 960,0 1000,40 1000,960 960,1000 40,1000 0,960 0,40\";\n-\n-  // Speed lines at top — 6 diagonal slashes near speaker side\n+  const mainBody = \"40,0 960,0 1000,40 1000,960 960,1000 40,1000 0,960 0,40\";\n   const speedLines = Array.from({ length: 6 }, (_, i) => {\n     const x = isLeft ? 80 + i * 65 : 920 - i * 65;\n     return `M${x},0 L${x - 45},70 L${x - 20},70 L${x + 25},0 Z`;\n   }).join(\" \");\n@@ -59,25 +47,17 @@\n           <stop offset=\"0%\" stopColor={fill} />\n           <stop offset=\"100%\" stopColor={fill} stopOpacity=\"0.85\" />\n         </linearGradient>\n       </defs>\n-\n-      {/* Main body */}\n       <polygon points={mainBody} fill={`url(#dialogGrad-${side})`} />\n-\n-      {/* Border glow */}\n       <polygon\n         points={mainBody}\n         fill=\"none\"\n         stroke={accent}\n         strokeWidth=\"2.5\"\n         strokeOpacity=\"0.6\"\n       />\n-\n-      {/* Speed lines at top */}\n       <path d={speedLines} fill={accentSecondary} fillOpacity=\"0.7\" />\n-\n-      {/* Inner border (subtle) */}\n       <polygon\n         points={mainBody}\n         fill=\"none\"\n         stroke=\"rgba(255,255,255,0.06)\"\n@@ -91,33 +71,49 @@\n export const Subtitle: React.FC<{\n   text: string;\n   speakerId: 0 | 1;\n   durationFrames: number;\n-}> = ({ text, speakerId, durationFrames }) => {\n+  /** Per-word timestamps from the JSON source (absolute seconds in audio) */\n+  words?: WordTiming[];\n+  /** Absolute start time of this segment in seconds — needed to compute relative word offsets */\n+  segmentStartSeconds?: number;\n+}> = ({ text, speakerId, durationFrames, words, segmentStartSeconds = 0 }) => {\n   const frame = useCurrentFrame();\n   const { fps } = useVideoConfig();\n   const theme = SPEAKER_THEME[speakerId];\n   const side = speakerId === 0 ? \"left\" : \"right\";\n \n-  // Split text into words\n-  const words = text.split(/\\s+/);\n-  const totalWords = words.length;\n+  // Current time position relative to segment start\n+  const currentSeconds = frame / fps + segmentStartSeconds;\n \n-  const revealEnd = Math.max(durationFrames * 0.85, 30);\n-  const wordsToShow = Math.floor(\n-    interpolate(frame, [10, revealEnd], [0, totalWords], {\n-      extrapolateLeft: \"clamp\",\n-      extrapolateRight: \"clamp\",\n-    }),\n-  );\n+  // Split text into displayable words (non-empty tokens)\n+  const textWords = text.split(/\\s+/).filter(Boolean);\n \n+  // Determine how many words are visible.\n+  // Mode A: if per-word timestamps are available, use them for exact sync.\n+  // Mode B: fallback to linear interpolation across segment duration.\n+  let wordsToShow: number;\n+  if (words && words.length > 0) {\n+    // Count words whose start_time has been reached\n+    wordsToShow = words.filter((w) => w.start_time <= currentSeconds).length;\n+    // Clamp to the number of rendered words in case word counts diverge\n+    wordsToShow = Math.min(wordsToShow, textWords.length);\n+  } else {\n+    const revealEnd = Math.max(durationFrames * 0.85, 30);\n+    wordsToShow = Math.floor(\n+      interpolate(frame, [10, revealEnd], [0, textWords.length], {\n+        extrapolateLeft: \"clamp\",\n+        extrapolateRight: \"clamp\",\n+      }),\n+    );\n+  }\n+\n   // Container entrance spring\n   const containerSpring = spring({\n     frame: frame - 5,\n     fps,\n     config: { damping: 18, stiffness: 120 },\n   });\n-\n   const containerY = interpolate(containerSpring, [0, 1], [40, 0]);\n   const containerOpacity = containerSpring;\n \n   // Exit fade\n@@ -150,10 +146,8 @@\n           accent={theme.accent}\n           accentSecondary={theme.accentSecondary}\n           side={side}\n         />\n-\n-        {/* Text content */}\n         <div\n           style={{\n             position: \"relative\",\n             zIndex: 1,\n@@ -174,12 +168,11 @@\n               flexWrap: \"wrap\",\n               gap: \"0 8px\",\n             }}\n           >\n-            {words.map((word, i) => {\n+            {textWords.map((word, i) => {\n               const isVisible = i < wordsToShow;\n               const isJustAppeared = i === wordsToShow - 1;\n-\n               return (\n                 <span\n                   key={i}\n                   style={{\n"
                }
            ],
            "date": 1774948956005,
            "name": "Commit-0",
            "content": "import React from \"react\";\nimport { interpolate, spring, useCurrentFrame, useVideoConfig } from \"remotion\";\n\nconst SPEAKER_BORDER_COLORS = {\n  0: \"#4facfe\",\n  1: \"#f093fb\",\n} as const;\n\nexport const Subtitle: React.FC<{\n  text: string;\n  speakerId: 0 | 1;\n  durationFrames: number;\n}> = ({ text, speakerId, durationFrames }) => {\n  const frame = useCurrentFrame();\n  const { fps } = useVideoConfig();\n\n  // Split text into words\n  const words = text.split(/\\s+/);\n  const totalWords = words.length;\n\n  // Calculate how many words should be visible at the current frame.\n  // Start revealing at frame 10, finish at 85% of the segment duration\n  // to give time for reading the last words.\n  const revealEnd = Math.max(durationFrames * 0.85, 30);\n  const wordsToShow = Math.floor(\n    interpolate(frame, [10, revealEnd], [0, totalWords], {\n      extrapolateLeft: \"clamp\",\n      extrapolateRight: \"clamp\",\n    })\n  );\n\n  // Container entrance spring\n  const containerSpring = spring({\n    frame: frame - 5,\n    fps,\n    config: { damping: 18, stiffness: 120 },\n  });\n\n  const containerY = interpolate(containerSpring, [0, 1], [40, 0]);\n  const containerOpacity = containerSpring;\n\n  // Exit fade\n  const exitOpacity = interpolate(\n    frame,\n    [durationFrames - 10, durationFrames],\n    [1, 0],\n    { extrapolateLeft: \"clamp\", extrapolateRight: \"clamp\" }\n  );\n\n  const borderColor = SPEAKER_BORDER_COLORS[speakerId];\n\n  return (\n    <div\n      style={{\n        position: \"absolute\",\n        bottom: 60,\n        left: 60,\n        right: 60,\n        transform: `translateY(${containerY}px)`,\n        opacity: containerOpacity * exitOpacity,\n      }}\n    >\n      <div\n        style={{\n          backgroundColor: \"rgba(4, 7, 18, 0.82)\",\n          backdropFilter: \"blur(8px)\",\n          WebkitBackdropFilter: \"blur(8px)\",\n          borderRadius: 16,\n          padding: \"28px 36px\",\n          borderLeft: `6px solid ${borderColor}`,\n          boxShadow: `0 0 30px ${borderColor}33, 0 8px 32px rgba(0,0,0,0.5), inset 0 0 0 1px rgba(255,255,255,0.06)`,\n        }}\n      >\n        <div\n          style={{\n            fontFamily: '\"Courier New\", \"JetBrains Mono\", monospace',\n            fontSize: 34,\n            color: \"white\",\n            lineHeight: 1.6,\n            display: \"flex\",\n            flexWrap: \"wrap\",\n            gap: \"0 8px\",\n          }}\n        >\n          {words.map((word, i) => {\n            const isVisible = i < wordsToShow;\n            const isJustAppeared = i === wordsToShow - 1;\n\n            return (\n              <span\n                key={i}\n                style={{\n                  opacity: isVisible ? 1 : 0.12,\n                  color: isVisible\n                    ? isJustAppeared\n                      ? borderColor\n                      : \"rgba(255,255,255,0.95)\"\n                    : \"rgba(255,255,255,0.1)\",\n                  fontWeight: isJustAppeared ? 700 : 500,\n                  textShadow: isJustAppeared ? `0 0 12px ${borderColor}, 0 0 24px ${borderColor}66` : \"none\",\n                  transition: \"color 0.1s\",\n                }}\n              >\n                {word}\n              </span>\n            );\n          })}}\n        </div>\n      </div>\n    </div>\n  );\n};\n"
        }
    ]
}